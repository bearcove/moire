[package]
name = "mutex-lock-order-inversion"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
peeps = { path = "../../crates/peeps", features = ["diagnostics"] }
tokio = { version = "1.49", features = ["macros", "rt-multi-thread", "signal", "sync", "time"] }
